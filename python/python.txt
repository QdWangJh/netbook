python变量
变量的类型
存储一个数据就需要一个变量
查看变量的类型 type（变量名字）
查看内存地址    id（）
删除变量  del（）
输入  input（）
输出  print（）

raw input（） 接收的数据都作为字符串  多用来输入注释
raw print（）将数据作为字符串输出
 
数字类型：整形 int 浮点型 float
取整  数字//数字

内建函数  abs（x）取x的绝对值
	round（number，ndigits=0）对数值进行四舍五入，ndigits是小数向右取整的位数，负数表示向左取整。（不严谨）
	pow（x，y，z=none）相当于x**y或x**y%z
帮助  help（函数名）